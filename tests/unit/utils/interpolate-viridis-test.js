import interpolateViridis from 'ember-fractals/utils/interpolate-viridis';
import { module, test } from 'qunit';

module('Unit | Utility | interpolate viridis');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = interpolateViridis();
  assert.ok(result);
});
